/**
 * Targeted drills — public entry point.
 *
 * One page. The typing itself is `@features/typing`, the text comes from
 * `@core/text`, and the measurement from `@core/telemetry`; this feature only
 * joins them up.
 */

export { DrillPage } from './pages/DrillPage.tsx'
